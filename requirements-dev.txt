# ==========================================================
# INGESTION-ONLY dependencies. Never installed on Vercel.
# Needed to run store_index.py on your machine.
#   pip install -r requirements.txt -r requirements-dev.txt
# ==========================================================
sentence-transformers==4.1.0
pypdf==5.6.1
langchain-openai==0.3.24
